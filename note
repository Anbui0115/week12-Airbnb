//to test api/test in dev console
// window
//   .csrfFetch("/api/test", {
//     method: "POST",
//     body: JSON.stringify({ credential: "Demo-lition", password: "password" }),
//   })
//   .then((res) => res.json())
//   .then((data) => console.log(data));

// //test log in in dev console
// window.store.dispatch(
//   window.sessionActions.login({
//     credential: "Demo-lition",
//     password: "password",
//   })
// );
// //test sign up in dev console
// window.store.dispatch(
//   window.sessionActions.signup({
//     username: "NewUser",
//     email: "new@user.io",
//     password: "password",
//   })
// );
// //test log out in dev console
// window.store.dispatch(window.sessionActions.logout());
// Proxy error: Could not proxy request /api/csrf/restore from localhost:3000 to http://localhost:8001.
// See https://nodejs.org/api/errors.html#errors_common_system_errors for more information (ECONNREFUSED).
/*
We might have to iterate through the review data before the return in the component function body
and just check if the current user left a review and save a true or false in a variable based off of that.
Then we can conditionally render the Leave a review button off of that.
*/
